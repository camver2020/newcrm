<UserControl x:Class="CampingCarCrm_Frontend.Views.ReservationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="10" VerticalAlignment="Top">
            <Calendar x:Name="ReservationCalendar"
                      SelectedDatesChanged="ReservationCalendar_SelectedDatesChanged"/>

            <Button x:Name="NewReservationButton" Content="선택 날짜에 새 예약" Margin="0,10,0,0" FontWeight="Bold" Click="NewReservationButton_Click"/>

            <Button x:Name="EditReservationButton" Content="선택 예약 수정" Margin="0,10,0,0" FontWeight="Bold" Click="EditReservationButton_Click" Background="#FFC107"/>
            <Button x:Name="DeleteReservationButton" Content="선택 예약 삭제" Margin="0,5,0,0" FontWeight="Bold" Click="DeleteReservationButton_Click" Background="#F44336" Foreground="White"/>
        </StackPanel>

        <DataGrid x:Name="ReservationDataGrid" Grid.Column="1" Margin="10" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="시간" Binding="{Binding StartDateTime, StringFormat='HH:mm'}" Width="Auto"/>
                <DataGridTextColumn Header="회원 이름" Binding="{Binding MemberName}" Width="*"/>
                <DataGridTextColumn Header="캠핑카" Binding="{Binding CarNickname}" Width="*"/>
                <DataGridTextColumn Header="상태" Binding="{Binding ReservationStatus}" Width="*"/>
                <DataGridTextColumn Header="담당자" Binding="{Binding ManagerName}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>