<UserControl x:Class="CampingCarCrm_Frontend.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White" Loaded="UserControl_Loaded">
    <Grid Margin="10">
        <TabControl>
            <TabItem Header="회사 관리">
                <Grid Margin="10">
                    <GroupBox Header="회사 목록" FontWeight="Bold" Padding="10">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Right" Margin="10,0,0,0">
                                <TextBlock Text="새 회사 이름:"/>
                                <TextBox x:Name="NewCompanyTextBox" Width="150" Margin="0,5,0,10"/>
                                <Button x:Name="AddCompanyButton" Content="추가" Click="AddCompanyButton_Click"/>
                            </StackPanel>
                            <Button x:Name="DeleteCompanyButton" Content="선택 항목 삭제" DockPanel.Dock="Bottom" Margin="0,10,0,0" VerticalAlignment="Bottom" Click="DeleteCompanyButton_Click" Background="IndianRed" Foreground="White"/>
                            <ListBox x:Name="CompanyListBox" DisplayMemberPath="CompanyName"/>
                        </DockPanel>
                    </GroupBox>
                </Grid>
            </TabItem>

            <TabItem Header="지점 관리">
                <Grid Margin="10">
                    <GroupBox Header="지점 목록" FontWeight="Bold" Padding="10">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Right" Margin="10,0,0,0">
                                <TextBlock Text="새 지점 이름:"/>
                                <TextBox x:Name="NewBranchTextBox" Width="150" Margin="0,5,0,10"/>
                                <Button x:Name="AddBranchButton" Content="추가" Click="AddBranchButton_Click"/>
                            </StackPanel>
                            <Button x:Name="DeleteBranchButton" Content="선택 항목 삭제" DockPanel.Dock="Bottom" Margin="0,10,0,0" VerticalAlignment="Bottom" Click="DeleteBranchButton_Click" Background="IndianRed" Foreground="White"/>
                            <ListBox x:Name="BranchListBox" DisplayMemberPath="BranchName"/>
                        </DockPanel>
                    </GroupBox>
                </Grid>
            </TabItem>

            <TabItem Header="담당자 관리">
                <Grid Margin="10">
                    <GroupBox Header="담당자 목록" FontWeight="Bold" Padding="10">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Right" Margin="10,0,0,0">
                                <TextBlock Text="새 담당자 이름:"/>
                                <TextBox x:Name="NewManagerTextBox" Width="150" Margin="0,5,0,10"/>
                                <Button x:Name="AddManagerButton" Content="추가" Click="AddManagerButton_Click"/>
                            </StackPanel>
                            <Button x:Name="DeleteManagerButton" Content="선택 항목 삭제" DockPanel.Dock="Bottom" Margin="0,10,0,0" VerticalAlignment="Bottom" Click="DeleteManagerButton_Click" Background="IndianRed" Foreground="White"/>
                            <ListBox x:Name="ManagerListBox" DisplayMemberPath="ManagerName"/>
                        </DockPanel>
                    </GroupBox>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>