<UserControl x:Class="CampingCarCrm_Frontend.Views.MemberView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Loaded="UserControl_Loaded" Background="White">
    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Right" Width="250" Margin="10">
                <TextBlock Text="회원 이름:" FontWeight="Bold"/>
                <TextBox x:Name="NameTextBox" Margin="0,0,0,10"/>
                <TextBlock Text="연락처:" FontWeight="Bold"/>
                <TextBox x:Name="ContactTextBox" Margin="0,0,0,10"/>
                <TextBlock Text="비상 연락처:" FontWeight="Bold"/>
                <TextBox x:Name="EmergencyContactTextBox" Margin="0,0,0,10"/>
                <TextBlock Text="소속 회사:" FontWeight="Bold"/>
                <TextBox x:Name="CompanyTextBox" Margin="0,0,0,10"/>
                <TextBlock Text="소속 지점:" FontWeight="Bold"/>
                <TextBox x:Name="BranchTextBox" Margin="0,0,0,10"/>
                <TextBlock Text="메모:" FontWeight="Bold"/>
                <TextBox x:Name="MemoTextBox" Margin="0,0,0,20" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                <Button Content="신규 회원 추가" Margin="0,0,0,10" Click="AddMemberButton_Click" Background="#4CAF50" Foreground="White"/>
                <Button Content="선택 회원 수정" Margin="0,0,0,10" Click="UpdateMemberButton_Click" Background="#FFC107" Foreground="White"/>
                <Button Content="선택 회원 삭제" Margin="0,0,0,10" Click="DeleteMemberButton_Click" Background="#F44336" Foreground="White"/>
            </StackPanel>
            <DataGrid x:Name="MemberDataGrid" FlowDirection="LeftToRight" AutoGenerateColumns="False" Margin="10" IsReadOnly="True" SelectionChanged="MemberDataGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding MemberID}" Width="50"/>
                    <DataGridTextColumn Header="회원 이름" Binding="{Binding MemberName}" Width="*"/>
                    <DataGridTextColumn Header="연락처" Binding="{Binding Contact}" Width="*"/>
                    <DataGridTextColumn Header="비상 연락처" Binding="{Binding EmergencyContact}" Width="*"/>
                    <DataGridTextColumn Header="소속 회사" Binding="{Binding CompanyName}" Width="*"/>
                    <DataGridTextColumn Header="소속 지점" Binding="{Binding BranchName}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>